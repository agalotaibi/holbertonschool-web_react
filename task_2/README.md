### Task 2: Advanced types
- Implements `Director` and `Teacher` classes with specific interfaces.
- Creates a factory function `createEmployee`.
- Uses Type Predicates (`isDirector`) and String Literal Types (`Subjects`).
