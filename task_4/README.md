### Task 4: Namespace & Declaration merging
- Organizes code using the `Subjects` namespace.
- Demonstrates declaration merging to extend the `Teacher` interface across multiple files (Cpp, React, Java).
