### Task 1: Building a Teacher interface
- Implements `Teacher` and `Directors` interfaces.
- Uses `readonly` and optional properties.
- Implements a function interface `printTeacherFunction`.
- Creates a `StudentClass` described by interfaces.
